{#
/**
 * @file
 * Default theme implementation for webform submission as YAML.
 *
 * Available variables:
 * - yaml: The webform submission's data as YAML.

 * @see template_preprocess_webform_submission_yaml()
 *
 * @ingroup themeable
 */
#}
{{ yaml }}
